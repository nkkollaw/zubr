<?php
declare(strict_types=1);

/**
 * \Zubr\similar_text()
 *
 * @link https://secure.php.net/similar_text
 *
 * NOTES: same as \similar_text() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function similar_text ($first, $second, &$percent = null)
{
    // TODO: implement
}