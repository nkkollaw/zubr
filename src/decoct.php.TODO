<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\decoct()
 *
 * @link https://secure.php.net/decoct
 *
 * NOTES: same as \decoct() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function decoct($number)
{
    return \decoct($number);
}