<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\array_reduce()
 *
 * @link https://secure.php.net/array_reduce
 *
 * NOTES: same as \array_reduce() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_reduce(array $input, $function, $initial = null)
{
    return \array_reduce();
}