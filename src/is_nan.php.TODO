<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\is_nan()
 *
 * @link https://secure.php.net/is_nan
 *
 * NOTES: same as \is_nan() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function is_nan($val)
{
    return \is_nan($val);
}