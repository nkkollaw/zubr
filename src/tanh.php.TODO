<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\tanh()
 *
 * @link https://secure.php.net/tanh
 *
 * NOTES: same as \tanh() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function tanh($arg)
{
    return \tanh($arg);
}