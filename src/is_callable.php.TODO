<?php
declare(strict_types=1);

/**
 * \Zubr\is_callable()
 *
 * @link https://secure.php.net/is_callable
 *
 * NOTES: same as \is_callable() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function is_callable ($var, $syntax_only = null, &$callable_name = null)
{
    // TODO: implement
}