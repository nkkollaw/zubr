<?php
declare(strict_types=1);

/**
 * \Zubr\range()
 *
 * @link https://secure.php.net/range
 *
 * NOTES: same as \range() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function range ($low, $high, $step = null)
{
    // TODO: implement
}