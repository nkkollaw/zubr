<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\rand()
 *
 * @link https://secure.php.net/rand
 *
 * NOTES: same as \rand() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function rand($min, $max)
{
    return \rand($min, $max);
}