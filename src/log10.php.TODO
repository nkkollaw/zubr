<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\log10()
 *
 * @link https://secure.php.net/log10
 *
 * NOTES: same as \log10() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function log10($arg)
{
    return \log10($arg);
}