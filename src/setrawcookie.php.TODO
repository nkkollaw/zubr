<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\setrawcookie()
 *
 * @link https://secure.php.net/setrawcookie
 *
 * NOTES: same as \setrawcookie() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function setrawcookie($name, $value = null, $expire = 0, $path = "", $domain = "", $secure = false, $httponly = false)
{
    return \setrawcookie($name, $value = null, $expire = 0, $path = "", $domain = "", $secure = false, $httponly = false);
}