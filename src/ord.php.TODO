<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\ord()
 *
 * @link https://secure.php.net/ord
 *
 * NOTES: same as \ord() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function ord($string)
{
    return \ord($string);
}