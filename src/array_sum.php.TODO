<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\array_sum()
 *
 * @link https://secure.php.net/array_sum
 *
 * NOTES: same as \array_sum() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_sum(array $array)
{
    return \array_sum();
}