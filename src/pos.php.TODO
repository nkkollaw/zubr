<?php
declare(strict_types=1);

/**
 * \Zubr\pos()
 *
 * @link https://secure.php.net/pos
 *
 * NOTES: same as \pos() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function pos(&$arg)
{
    // TODO: implement
}