<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\log1p()
 *
 * @link https://secure.php.net/log1p
 *
 * NOTES: same as \log1p() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function log1p($number)
{
    return \log1p($number);
}