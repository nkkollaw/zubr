<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\mt_rand()
 *
 * @link https://secure.php.net/mt_rand
 *
 * NOTES: same as \mt_rand() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function mt_rand($min, $max)
{
    return \mt_rand($min, $max);
}