<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\ini_get()
 *
 * @link https://secure.php.net/ini_get
 *
 * NOTES: same as \ini_get() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function ini_get($varname)
{
    return \ini_get($varname);
}