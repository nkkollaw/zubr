<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\natsort()
 *
 * @link https://secure.php.net/natsort
 *
 * NOTES: same as \natsort() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function natsort(array &$array)
{
    return \natsort(array &$array);
}