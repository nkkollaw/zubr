<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\is_object()
 *
 * @link https://secure.php.net/is_object
 *
 * NOTES: same as \is_object() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function is_object($var)
{
    return \is_object($var);
}