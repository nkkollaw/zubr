<?php
declare(strict_types=1);

/**
 * \Zubr\array_reverse()
 *
 * @link https://secure.php.net/array_reverse
 *
 * NOTES: same as \array_reverse() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_reverse(array $array, $preserve_keys = null)
{
    // TODO: implement
}