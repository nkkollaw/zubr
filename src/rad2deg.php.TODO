<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\rad2deg()
 *
 * @link https://secure.php.net/rad2deg
 *
 * NOTES: same as \rad2deg() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function rad2deg($number)
{
    return \rad2deg($number);
}