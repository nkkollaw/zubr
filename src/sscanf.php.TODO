<?php
declare(strict_types=1);

/**
 * \Zubr\sscanf()
 *
 * @link https://secure.php.net/sscanf
 *
 * NOTES: same as \sscanf() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function sscanf ($str, $format, &$_ = null)
{
    // TODO: implement
}