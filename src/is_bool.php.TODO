<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\is_bool()
 *
 * @link https://secure.php.net/is_bool
 *
 * NOTES: same as \is_bool() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function is_bool($var)
{
    return \is_bool($var);
}