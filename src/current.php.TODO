<?php
declare(strict_types=1);

/**
 * \Zubr\current()
 *
 * @link https://secure.php.net/current
 *
 * NOTES: same as \current() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function current (array &$array)
{
    // TODO: implement
}