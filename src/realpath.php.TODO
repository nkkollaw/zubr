<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\realpath()
 *
 * @link https://secure.php.net/realpath
 *
 * NOTES: same as \realpath() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function realpath($path)
{
    return \realpath($path);
}