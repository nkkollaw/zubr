<?php
declare(strict_types=1);

/**
 * \Zubr\pack()
 *
 * @link https://secure.php.net/pack
 *
 * NOTES: same as \pack() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function pack ($format, $args = null, $_ = null)
{
    // TODO: implement
}