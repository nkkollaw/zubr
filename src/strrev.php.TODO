<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\strrev()
 *
 * @link https://secure.php.net/strrev
 *
 * NOTES: same as \strrev() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function strrev($string)
{
    return \strrev($string);
}