<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\array_merge_recursive()
 *
 * @link https://secure.php.net/array_merge_recursive
 *
 * NOTES: same as \array_merge_recursive() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_merge_recursive(array $array1, array $_ = null)
{
    return \array_merge_recursive();
}