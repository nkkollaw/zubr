<?php
declare(strict_types=1);

/**
 * \Zubr\bin2hex()
 *
 * @link https://secure.php.net/bin2hex
 *
 * NOTES: same as \bin2hex() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function bin2hex ($str)
{
    // TODO: implement
}