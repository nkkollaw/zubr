<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\soundex()
 *
 * @link https://secure.php.net/soundex
 *
 * NOTES: same as \soundex() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function soundex($str)
{
    return \soundex($str);
}