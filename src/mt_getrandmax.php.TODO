<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\mt_getrandmax()
 *
 * @link https://secure.php.net/mt_getrandmax
 *
 * NOTES: same as \mt_getrandmax() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function mt_getrandmax()
{
    return \mt_getrandmax();
}