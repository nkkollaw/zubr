<?php
declare(strict_types=1);

/**
 * \Zubr\substr_count()
 *
 * @link https://secure.php.net/substr_count
 *
 * NOTES: same as \substr_count() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function substr_count ($haystack, $needle, $offset = null, $length = null)
{
    // TODO: implement
}