<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\is_finite()
 *
 * @link https://secure.php.net/is_finite
 *
 * NOTES: same as \is_finite() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function is_finite($val)
{
    return \is_finite($val);
}