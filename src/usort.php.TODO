<?php
declare(strict_types=1);

/**
 * \Zubr\usort()
 *
 * @link https://secure.php.net/usort
 *
 * NOTES: same as \usort() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function usort (array &$array, $cmp_function)
{
    // TODO: implement
}