<?php
declare(strict_types=1);

/**
 * \Zubr\pow()
 *
 * @link https://secure.php.net/pow
 *
 * NOTES: same as \pow() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function pow ($base, $exp)
{
    // TODO: implement
}