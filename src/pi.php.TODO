<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\pi()
 *
 * @link https://secure.php.net/pi
 *
 * NOTES: same as \pi() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function pi()
{
    return \pi();
}