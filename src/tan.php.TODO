<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\tan()
 *
 * @link https://secure.php.net/tan
 *
 * NOTES: same as \tan() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function tan($arg)
{
    return \tan($arg);
}