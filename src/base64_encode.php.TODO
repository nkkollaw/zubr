<?php
declare(strict_types=1);

/**
 * \Zubr\base64_encode()
 *
 * @link https://secure.php.net/base64_encode
 *
 * NOTES: same as \base64_encode() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function base64_encode ($data)
{
    // TODO: implement
}