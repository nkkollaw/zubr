<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\prev()
 *
 * @link https://secure.php.net/prev
 *
 * NOTES: same as \prev() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function prev(array &$array)
{
    return \prev(array &$array);
}