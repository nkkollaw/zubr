<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\array_udiff()
 *
 * @link https://secure.php.net/array_udiff
 *
 * NOTES: same as \array_udiff() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_udiff(array $array1, array $array2, array $_ = null, $data_compare_func)
{
    return \array_udiff();
}