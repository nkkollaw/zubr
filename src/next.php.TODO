<?php
declare(strict_types=1);

/**
 * \Zubr\next()
 *
 * @link https://secure.php.net/next
 *
 * NOTES: same as \next() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function next (array &$array)
{
    // TODO: implement
}