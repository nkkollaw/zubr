<?php
declare(strict_types=1);

/**
 * \Zubr\rsort()
 *
 * @link https://secure.php.net/rsort
 *
 * NOTES: same as \rsort() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function rsort (array &$array, $sort_flags = null)
{
    // TODO: implement
}