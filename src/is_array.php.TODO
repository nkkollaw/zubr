<?php
declare(strict_types=1);

/**
 * \Zubr\is_array()
 *
 * @link https://secure.php.net/is_array
 *
 * NOTES: same as \is_array() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function is_array ($var)
{
    // TODO: implement
}