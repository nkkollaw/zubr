<?php
declare(strict_types=1);

/**
 * \Zubr\sort()
 *
 * @link https://secure.php.net/sort
 *
 * NOTES: same as \sort() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function sort (array &$array, $sort_flags = null)
{
    // TODO: implement
}