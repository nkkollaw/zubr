<?php
declare(strict_types=1);

/**
 * \Zubr\array_udiff_uassoc()
 *
 * @link https://secure.php.net/array_udiff_uassoc
 *
 * NOTES: same as \array_udiff_uassoc() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_udiff_uassoc(array $array1, array $array2, array $_ = null, $data_compare_func, $key_compare_func)
{
    // TODO: implement
}