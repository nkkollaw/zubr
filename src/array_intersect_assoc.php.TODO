<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\array_intersect_assoc()
 *
 * @link https://secure.php.net/array_intersect_assoc
 *
 * NOTES: same as \array_intersect_assoc() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_intersect_assoc(array $array1, array $array2, array $_ = null)
{
    return \array_intersect_assoc();
}