<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\array_diff_ukey()
 *
 * @link https://secure.php.net/array_diff_ukey
 *
 * NOTES: same as \array_diff_ukey() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_diff_ukey(array $array1, array $array2, array $_ = null, $key_compare_func)
{
    return \array_diff_ukey();
}