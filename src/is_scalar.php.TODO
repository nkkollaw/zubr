<?php
declare(strict_types=1);

/**
 * \Zubr\is_scalar()
 *
 * @link https://secure.php.net/is_scalar
 *
 * NOTES: same as \is_scalar() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function is_scalar ($var)
{
    // TODO: implement
}