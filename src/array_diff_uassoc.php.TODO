<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\array_diff_uassoc()
 *
 * @link https://secure.php.net/array_diff_uassoc
 *
 * NOTES: same as \array_diff_uassoc() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_diff_uassoc(array $array1, array $array2, array $_ = null, $key_compare_func)
{
    return \array_diff_uassoc();
}