<?php
declare(strict_types=1);

/**
 * \Zubr\htmlspecialchars()
 *
 * @link https://secure.php.net/htmlspecialchars
 *
 * NOTES: same as \htmlspecialchars() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function htmlspecialchars ($string, $flags = ENT_COMPAT, $encoding = 'UTF-8', $double_encode = true)
{
    // TODO: implement
}