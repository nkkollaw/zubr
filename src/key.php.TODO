<?php
declare(strict_types=1);

/**
 * \Zubr\key()
 *
 * @link https://secure.php.net/key
 *
 * NOTES: same as \key() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function key (array &$array)
{
    // TODO: implement
}