<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\hypot()
 *
 * @link https://secure.php.net/hypot
 *
 * NOTES: same as \hypot() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function hypot($x, $y)
{
    return \hypot($x, $y);
}