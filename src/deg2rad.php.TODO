<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\deg2rad()
 *
 * @link https://secure.php.net/deg2rad
 *
 * NOTES: same as \deg2rad() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function deg2rad($number)
{
    return \deg2rad($number);
}