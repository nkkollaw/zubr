<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\strval()
 *
 * @link https://secure.php.net/strval
 *
 * NOTES: same as \strval() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function strval($var)
{
    return \strval($var);
}