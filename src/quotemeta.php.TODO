<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\quotemeta()
 *
 * @link https://secure.php.net/quotemeta
 *
 * NOTES: same as \quotemeta() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function quotemeta($str)
{
    return \quotemeta($str);
}