<?php
declare(strict_types=1);

/**
 * \Zubr\array_replace()
 *
 * @link https://secure.php.net/array_replace
 *
 * NOTES: same as \array_replace() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function array_replace(array $array, array $array1, array $array2 = null, array $_ = null)
{
    // TODO: implement
}