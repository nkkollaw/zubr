<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\end()
 *
 * @link https://secure.php.net/end
 *
 * NOTES: same as \end() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function end(array &$array)
{
    return \end(array &$array);
}