<?php
declare(strict_types=1);

/**
 * \Zubr\asort()
 *
 * @link https://secure.php.net/asort
 *
 * NOTES: same as \asort() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function asort (array &$array, $sort_flags = null)
{
    // TODO: implement
}