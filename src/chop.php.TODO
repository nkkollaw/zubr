<?php
declare(strict_types=1);

/**
 * \Zubr\chop()
 *
 * @link https://secure.php.net/chop
 *
 * NOTES: same as \chop() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function chop ($str, $character_mask)
{
    // TODO: implement
}