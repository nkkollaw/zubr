<?php
declare(strict_types=1);

/**
 * \Zubr\is_real()
 *
 * @link https://secure.php.net/is_real
 *
 * NOTES: same as \is_real() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function is_real ($var)
{
    // TODO: implement
}