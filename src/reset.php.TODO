<?php
declare(strict_types=1);

namespace Zubr;

/**
 * \Zubr\reset()
 *
 * @link https://secure.php.net/reset
 *
 * NOTES: same as \reset() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function reset(array &$array)
{
    return \reset(array &$array);
}