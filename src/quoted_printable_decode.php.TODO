<?php
declare(strict_types=1);

/**
 * \Zubr\quoted_printable_decode()
 *
 * @link https://secure.php.net/quoted_printable_decode
 *
 * NOTES: same as \quoted_printable_decode() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function quoted_printable_decode ($str)
{
    // TODO: implement
}