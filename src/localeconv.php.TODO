<?php
declare(strict_types=1);

/**
 * \Zubr\localeconv()
 *
 * @link https://secure.php.net/localeconv
 *
 * NOTES: same as \localeconv() || in Zubr, $param_1 goes before $param_2, and we return an array instead of void
*/
function localeconv ()
{
    // TODO: implement
}