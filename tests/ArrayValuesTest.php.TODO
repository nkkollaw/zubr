<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => array_values( {firstname: 'Kevin', surname: 'van Zonneveld'} )
    [returns] => [ 'Kevin', 'van Zonneveld' ]
)
*/
class ArrayValues extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\array_values(/* put stuff here */);
        $expected = \array_values(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}