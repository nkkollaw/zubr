<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => soundex('Kevin')
    [returns] => 'K150'
)
Array
(
    [code] => soundex('Ellery')
    [returns] => 'E460'
)
Array
(
    [code] => soundex('Euler')
    [returns] => 'E460'
)
*/
class Soundex extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\soundex(/* put stuff here */);
        $expected = \soundex(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}