<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => implode(' ', ['Kevin', 'van', 'Zonneveld'])
    [returns] => 'Kevin van Zonneveld'
)
Array
(
    [code] => implode(' ', {first:'Kevin', last: 'van Zonneveld'})
    [returns] => 'Kevin van Zonneveld'
)
*/
class Implode extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\implode(/* put stuff here */);
        $expected = \implode(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}