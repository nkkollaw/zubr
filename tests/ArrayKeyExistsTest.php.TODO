<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => array_key_exists('kevin', {'kevin': 'van Zonneveld'})
    [returns] => true
)
*/
class ArrayKeyExists extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\array_key_exists(/* put stuff here */);
        $expected = \array_key_exists(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}