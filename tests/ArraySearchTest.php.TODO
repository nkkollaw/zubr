<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => array_search('zonneveld', {firstname: 'kevin', middle: 'van', surname: 'zonneveld'})
    [returns] => 'surname'
)
*/
class ArraySearch extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\array_search(/* put stuff here */);
        $expected = \array_search(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}