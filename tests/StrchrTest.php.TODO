<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => strchr('Kevin van Zonneveld', 'van')
    [returns] => 'van Zonneveld'
)
Array
(
    [code] => strchr('Kevin van Zonneveld', 'van', true)
    [returns] => 'Kevin '
)
*/
class Strchr extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\strchr(/* put stuff here */);
        $expected = \strchr(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}