<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => sizeof([[0,0],[0,-4]], 'COUNT_RECURSIVE')
    [returns] => 6
)
Array
(
    [code] => sizeof({'one' : [1,2,3,4,5]}, 'COUNT_RECURSIVE')
    [returns] => 6
)
*/
class Sizeof extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\sizeof(/* put stuff here */);
        $expected = \sizeof(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}