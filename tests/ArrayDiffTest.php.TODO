<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => array_diff(['Kevin', 'van', 'Zonneveld'], ['van', 'Zonneveld'])
    [returns] => {0:'Kevin'}
)
*/
class ArrayDiff extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\array_diff(/* put stuff here */);
        $expected = \array_diff(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}