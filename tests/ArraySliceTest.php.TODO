<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => array_slice(["a", "b", "c", "d", "e"], 2, -1)
    [returns] => [ 'c', 'd' ]
)
Array
(
    [code] => array_slice(["a", "b", "c", "d", "e"], 2, -1, true)
    [returns] => {2: 'c', 3: 'd'}
)
*/
class ArraySlice extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\array_slice(/* put stuff here */);
        $expected = \array_slice(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}