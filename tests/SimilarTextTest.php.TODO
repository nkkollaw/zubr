<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => similar_text('Hello World!', 'Hello locutus!')
    [returns] => 8
)
Array
(
    [code] => similar_text('Hello World!', null)
    [returns] => 0
)
*/
class SimilarText extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\similar_text(/* put stuff here */);
        $expected = \similar_text(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}