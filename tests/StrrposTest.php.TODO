<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => strrpos('Kevin van Zonneveld', 'e')
    [returns] => 16
)
Array
(
    [code] => strrpos('somepage.com', '.', false)
    [returns] => 8
)
Array
(
    [code] => strrpos('baa', 'a', 3)
    [returns] => false
)
Array
(
    [code] => strrpos('baa', 'a', 2)
    [returns] => 2
)
*/
class Strrpos extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\strrpos(/* put stuff here */);
        $expected = \strrpos(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}