<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => array_diff_key({red: 1, green: 2, blue: 3, white: 4}, {red: 5})
    [returns] => {"green":2, "blue":3, "white":4}
)
Array
(
    [code] => array_diff_key({red: 1, green: 2, blue: 3, white: 4}, {red: 5}, {red: 5})
    [returns] => {"green":2, "blue":3, "white":4}
)
*/
class ArrayDiffKey extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\array_diff_key(/* put stuff here */);
        $expected = \array_diff_key(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}