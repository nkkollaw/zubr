<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => array_unique(['Kevin','Kevin','van','Zonneveld','Kevin'])
    [returns] => {0: 'Kevin', 2: 'van', 3: 'Zonneveld'}
)
Array
(
    [code] => array_unique({'a': 'green', 0: 'red', 'b': 'green', 1: 'blue', 2: 'red'})
    [returns] => {a: 'green', 0: 'red', 1: 'blue'}
)
*/
class ArrayUnique extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\array_unique(/* put stuff here */);
        $expected = \array_unique(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}