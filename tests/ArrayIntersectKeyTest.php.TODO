<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => var $array1 = {a: 'green', b: 'brown', c: 'blue', 0: 'red'}
var $array2 = {a: 'green', 0: 'yellow', 1: 'red'}
array_intersect_key($array1, $array2)
    [returns] => {0: 'red', a: 'green'}
)
*/
class ArrayIntersectKey extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\array_intersect_key(/* put stuff here */);
        $expected = \array_intersect_key(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}