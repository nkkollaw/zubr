<?php
declare(strict_types=1);

/* These are tests from Locutus for porting
TODO: implement adding automatically (if it's possible)

Array
(
    [code] => array_diff_assoc({0: 'Kevin', 1: 'van', 2: 'Zonneveld'}, {0: 'Kevin', 4: 'van', 5: 'Zonneveld'})
    [returns] => {1: 'van', 2: 'Zonneveld'}
)
*/
class ArrayDiffAssoc extends PHPUnit\Framework\TestCase
{
    public function test()
    {
        $input = ['test1', 'tEsT2', 'TEST3'];
        $actual = \Zubr\array_diff_assoc(/* put stuff here */);
        $expected = \array_diff_assoc(/* put stuff here */);

        $this->assertEquals($expected, $actual);
    }
}